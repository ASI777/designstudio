# Gateway runtime (CPU/mock-mode — testable anywhere, no GPU).
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
httpx>=0.27          # FastAPI TestClient
# vLLM + PyTorch-ROCm are installed in Dockerfile.rocm (MI300X only), not here.
